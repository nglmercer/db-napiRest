{
  "name": "mediabunny-service",
  "version": "1.0.0",
  "description": "Video processing service with HTTP, WebSocket, and IPC support",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "build": "bun build src/index.ts --outdir dist"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "hono": "^4.6.14",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/ws": "^8.5.13"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
